from django.urls import path

from . import views


app_name = "exports_center"


urlpatterns = [
    # ==========================================================
    # لوحة مركز التصدير
    # ==========================================================
    path(
        "",
        views.dashboard_view,
        name="dashboard",
    ),
    # ==========================================================
    # فلاتر التقارير
    # ==========================================================
    path(
        "report/<str:report_key>/filters/",
        views.filters_view,
        name="filters",
    ),
    # ==========================================================
    # المعاينة التقليدية
    # ==========================================================
    path(
        "report/<str:report_key>/preview/",
        views.preview_view,
        name="preview",
    ),
    # ==========================================================
    # بيانات المعاينة عبر Ajax
    # ==========================================================
    path(
        "report/<str:report_key>/preview/data/",
        views.preview_data_view,
        name="preview-data",
    ),
    # ==========================================================
    # تنفيذ التصدير
    # ==========================================================
    path(
        ("report/<str:report_key>/export/<str:export_format>/"),
        views.export_view,
        name="export",
    ),
    # ==========================================================
    # استقبال نموذج التصدير الموحد
    # ==========================================================
    path(
        "export/submit/",
        views.export_submit_view,
        name="export-submit",
    ),
    # ==========================================================
    # سجل عمليات التصدير
    # ==========================================================
    path(
        "logs/",
        views.logs_view,
        name="logs",
    ),
    # ==========================================================
    # تنزيل ملف محفوظ من سجل التصدير
    # ==========================================================
    path(
        "logs/<int:export_log_id>/download/",
        views.download_export_view,
        name="download-export",
    ),
]
