# سياسة أمن منصة أبواب

## الإبلاغ عن ثغرة

لا تُنشر تفاصيل الثغرات أو بيانات التشغيل في التذاكر العامة. أرسل البلاغ إلى مسؤول النظام موضحًا المسار المتأثر، خطوات إعادة آمنة، والأثر المتوقع، من دون تضمين كلمات مرور أو بيانات شخصية.

## ضوابط الإنتاج الإلزامية

- تشغيل `DJANGO_DEBUG=false`.
- استخدام مفتاح عشوائي بطول 64 حرفًا أو أكثر عبر `DJANGO_SECRET_KEY`.
- استخدام PostgreSQL وتخزين كلمة مروره خارج المستودع.
- استخدام HTTPS فقط مع HSTS بعد التأكد من جاهزية النطاقات الفرعية.
- استخدام Redis مشتركًا لتحديد محاولات تسجيل الدخول بين العمال.
- إبقاء `ALLOW_PUBLIC_REGISTRATION=false` وإنشاء الحسابات عبر مدير النظام.
- عدم تخزين قاعدة البيانات أو الوسائط أو النسخ الاحتياطية داخل Git.
- تقييد الوصول إلى لوحة الإدارة على شبكة موثوقة أو VPN متى أمكن.

## فحوص دورية

```powershell
python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py test
python manage.py check --deploy
```

يجب تدوير الأسرار فورًا عند الاشتباه بتسربها، ومراجعة سجل نشاط النظام وتغييرات الصلاحيات والحسابات الإدارية.
