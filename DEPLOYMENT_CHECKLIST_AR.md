# قائمة اعتماد الإطلاق

## قبل بناء النسخة

- [ ] نسخ `.env.example` إلى مخزن أسرار آمن وتعبئة القيم الحقيقية.
- [ ] إزالة `db.sqlite3` من تتبع Git باستخدام `git rm --cached db.sqlite3` مع إبقاء الملف المحلي.
- [ ] التأكد من عدم وجود أسرار أو ملفات وسائط في تاريخ المستودع.
- [ ] تثبيت `requirements-production.txt` في بيئة نظيفة.
- [ ] تشغيل الاختبارات وفحص الترحيلات دون أخطاء.

## البنية التحتية

- [ ] PostgreSQL مخصص مع اتصال TLS وصلاحيات محدودة للتطبيق.
- [ ] Redis خاص غير مكشوف للإنترنت.
- [ ] خادم عكسي ينهي HTTPS ويرسل `X-Forwarded-Proto` بصورة موثوقة.
- [ ] تخزين دائم وآمن للوسائط مع نسخ احتياطي مشفر.
- [ ] جمع سجلات stdout/stderr مع تنبيه للأخطاء والحالات 5xx.
- [ ] فحص `/health/` من موازن الحمل دون نشر تفاصيل داخلية.

## قاعدة البيانات

- [ ] أخذ نسخة احتياطية قبل الترحيلات.
- [ ] تشغيل `python manage.py migrate --plan` ثم `python manage.py migrate`.
- [ ] تجربة الاستعادة من نسخة احتياطية في بيئة منفصلة.
- [ ] توثيق زمن الاستعادة والحد المقبول لفقد البيانات.

## القبول الأمني

- [ ] التسجيل العام مغلق.
- [ ] لا توجد حسابات افتراضية أو كلمات مرور مؤقتة غير منتهية.
- [ ] مديرو النظام فقط يصلون إلى `/admin/` ومركز النظام.
- [ ] تحديد محاولات الدخول يعمل عبر أكثر من عامل تطبيق.
- [ ] رفع الملفات يرفض المحتوى المخالف للامتداد.
- [ ] اختبار صفحات 403 و404 و500 دون تسريب تفاصيل.
- [ ] مراجعة `ALLOWED_HOSTS` و`CSRF_TRUSTED_ORIGINS` والنطاق الفعلي.

## قرار الإطلاق

لا يتم الإطلاق إذا فشل اختبار، أو تعذرت استعادة النسخة الاحتياطية، أو ظهرت تحذيرات من `check --deploy`. يسجل صاحب القرار ووقت الاعتماد وإصدار التطبيق في محضر الإطلاق.
